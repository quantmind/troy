#!/usr/bin/env bash

FMT_ARG="--check"
CLIPPY_ARG="-- -Dwarnings"

if [ "$1" = "fix" ] ; then
    FMT_ARG=""
fi


# without this the script exits with clippy's status alone, so a formatting
# failure in --check mode passes CI silently
set -e

cargo fmt ${FMT_ARG}
cargo clippy ${CLIPPY_ARG}
